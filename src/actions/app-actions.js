import * as types from './action-types';

var Home = {
    type: types.APP_HOME_SCREEN
}

var Student = {
    type: types.APP_STUDENT_SCREEN
}

var Lecturer = {
    type: types.APP_LECTURER_SCREEN
}

export default {
    Home,
    Student,
    Lecturer
}
