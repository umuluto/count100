import * as types from './action-types';

var Simplifying = {
    type: types.LECTURER_SIMPLIFY
}

var WhenToSimplify = {
    type: types.LECTURER_WHEN_TO_SIMPLIFY
}

export default {
    Simplifying,
    WhenToSimplify
}